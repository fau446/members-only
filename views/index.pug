extends layout

block content
  h1= title
  p Welcome to #{title}

  if post_list.length 
    ul 
      each post in post_list 
        li 
          p #{post.date}
          p #{post.user.name}
          p #{post.message}
  else
    p There are no posts.